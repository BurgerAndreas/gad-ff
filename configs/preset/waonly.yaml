# @package _global_

model:
  alpha_drop:               0.0         # [0.0, 0.1]
  drop_path_rate:           0.0         # [0.0, 0.05]
  proj_drop:                0.0

# Step decay learning rate schedule
training:
  
  hessian_loss_weight: 0.0

  mask_hessian: false
  eigen_loss:
    loss_name: "eigenspectrum"
    loss_type: "wa" # eigen or wa
    k: null
    alpha: 1.0
    dof_filter_fn: null
