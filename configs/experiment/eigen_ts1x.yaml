# @package _global_

# Fit on the larger training dataset (TS1x), test on TS1x

training:
  trn_path: "ts1x_hess_train_big-dft-hess-eigen.lmdb"
  val_path: "ts1x-val-dft-hess-eigen.lmdb"
  bz: 128
  
pltrainer:
  val_check_interval: 100    # Validate less frequently

experiment_name: "ts1x"