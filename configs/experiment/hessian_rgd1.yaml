# @package _global_

# Fit on the smaller training dataset (RGD1), test on TS1x

training:
  trn_path: "RGD1.lmdb"
  val_path: "ts1x-val.lmdb"
  # with DDP (Distributed Data Parallel), each GPU will get batch size many samples
  bz: 64
  num_workers: 16 # 48
  
# pltrainer:
#   val_check_interval: 100    # Validate less frequently

experiment_name: "hessian_rgd1"